server:
  port:
    2010

feign:
  hystrix:
    enabled:
     true

hystrix:
  command:
    default:
      execution:
        isolation:
          thread:
            timeoutInMilliseconds: 60000
            
eureka:
  client:
    registryFetchIntervalSeconds: 5
    
endpoints:
  routes:
    sensitive: false

management:
  security:
    enabled:
      false

foo:
  foo version 25
